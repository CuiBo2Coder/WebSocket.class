select t1.* from(select rownum rn, t2.* from ( select distinct c.contNo, busiNum,bankName,a.appid,c.agentCode,v.beginDate,v.endDate, d.codeName,c.officecode,c.bankShortName,d.othersign,c.bankCode,c.interactive,c.lastone from lscont c  left join lsappnt a on c.contNo=a.contNo left join  ldcode  d on d.code=c.interactive left join  lsvideo v  on c.contNo=v.contNo left join lsproductinfo p on c.contNo = p.contNo where 1=1 and c.lastone = 'Y'"